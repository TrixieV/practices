package sortandmerge;

public class Listy {
//    private static int binarySearch(List<Integer> list, int x, int left, int right) {
//        int mid = left + (right - left) / 2;
//        if (left > right) return -1;
//        if (list.elementAt(mid) == x) return mid;
//        else if (list.elementAt(mid) == -1 || list.elementAt(mid) > x) {
//            return binarySearch(list, x, left, mid - 1);
//        }
//        else {
//            return binarySearch(list, x, mid + 1, right);
//        }
//    }
//
//    private static int searchListy(List<Integer> list, int x) {
//        int index = 1;
//        while (list.elementAt(index) != -1 && list.elementAt(index) < x) {
//            index *= 2;
//        }
//        return binarySearch(list, x, index / 2, index);
//    }
}
